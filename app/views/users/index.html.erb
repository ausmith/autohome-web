<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<table>
<% @users.each do |u| %>
	<tr>
		<td><%= u.first_name %> <%= u.last_name %></td>
		<td><%= link_to "Profile", user_path(u) %></td>
	</tr>
<% end %>
</table>