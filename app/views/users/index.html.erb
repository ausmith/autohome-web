<h1>Users#index</h1>

<table class="table table-striped">
  <% @users.each do |u| %>
    <tr>
    <td><%= u.first_name %> <%= u.last_name %></td>
    <td><%= link_to "Profile", user_path(u) %></td>
  </tr>
<% end %>
</table>
